# ==================== Веб-сервер ====================
Flask==2.3.2
Flask-Cors==4.0.0  # Обновлено для совместимости с Python 3.10+ (collections.Iterable -> collections.abc.Iterable)

# ==================== TensorFlow и Keras (для моделей изображений и музыки) ====================
# TensorFlow 2.10.1 - совместим с Python 3.10 и моделями .h5
# ВАЖНО: tensorflow-cpu==2.10.1 больше не доступен в PyPI для новой установки
# 
# РЕШЕНИЕ: Если версия уже установлена в venv (проверьте: pip show tensorflow-cpu),
#          закомментируйте строку ниже и установите вручную:
#          pip install tensorflow-cpu==2.10.1
#
# Если нужно установить заново, используйте команду:
# pip install tensorflow-cpu==2.10.1 --no-deps
# или установите вручную перед запуском pip install -r requirements.txt

# Для Windows без GPU (версия уже должна быть установлена в venv):
# tensorflow-cpu==2.10.1  # Раскомментируйте, если версия уже установлена

# Если у вас есть GPU с CUDA:
# tensorflow==2.10.1

# ==================== PyTorch (для модели текста) ====================
# PyTorch 2.3.1 - совместим с Python 3.10 и numpy 1.26
# ВАЖНО: Устанавливайте из venv: .venv\Scripts\pip.exe install -r requirements.txt
# Или активируйте venv: .venv\Scripts\Activate.ps1
torch==2.3.1

# ==================== QRNN для текстовой модели ====================
# torchqrnn - устанавливается из локального проекта
# Установка: cd C:\Users\psobo\PycharmProjects\pytorch-qrnn && pip install -e .
# Или: pip install -e C:\Users\psobo\PycharmProjects\pytorch-qrnn
# torchqrnn не доступен в PyPI, поэтому установите вручную перед запуском сервера

# ==================== Численные библиотеки ====================
# NumPy 1.26.4 - совместим с TensorFlow 2.10, PyTorch 2.3 и scipy 1.10
numpy==1.26.4

# SciPy 1.10.1 - совместим с numpy 1.26 и Python 3.10
scipy==1.10.1

# ==================== Обработка изображений ====================
# Pillow 10.4.0 - имеет готовые бинарные колеса для Python 3.10 на Windows
Pillow==10.4.0

# ==================== Обработка аудио (для модели музыки) ====================
librosa==0.9.2
soundfile==0.11.0
# numba и llvmlite - зависимости librosa (совместимы с numpy 1.26)
numba>=0.57.0,<0.60.0
llvmlite>=0.40.0,<0.42.0

# ==================== Машинное обучение ====================
# scikit-learn - для TfidfVectorizer, LabelEncoder в текстовой модели
scikit-learn==1.1.3

# joblib - для сохранения/загрузки моделей (vectorizer, label_encoder)
joblib==1.2.0

# ==================== Работа с данными ====================
# pandas - для работы с CSV в текстовой модели
pandas>=1.5.0,<2.0.0

# tqdm - для прогресс-баров (используется в текстовой модели)
tqdm>=4.64.0

# ==================== Работа с HDF5 (для моделей .h5) ====================
# h5py 3.8.0 - совместим с TensorFlow 2.10 и Python 3.10
h5py==3.8.0

# ==================== Системные зависимости ====================
# cffi - требуется для soundfile и других библиотек
cffi>=1.15.0

# ==================== Опциональные зависимости (для GPU) ====================
# CuPy - НЕ ОБЯЗАТЕЛЬНО для работы проекта
# Используется только если нужна поддержка GPU для PyTorch/CuPy операций
# ВАЖНО: cupy-cpu больше не доступен в PyPI для Python 3.10
# Если нужен CuPy, установите вручную из другого источника или используйте GPU версию

# Если есть GPU с CUDA 12.x:
# cupy-cuda12x==12.6.0
# Если есть GPU с CUDA 11.x:
# cupy-cuda11x==12.6.0
# Если нет GPU (CPU-only) - cupy-cpu больше не доступен в PyPI:
# cupy-cpu==12.6.0  # Закомментировано - не доступен

# pynvrtc - обычно устанавливается автоматически с cupy
# pynvrtc>=9.2.0  # Закомментировано - не используется в проекте

# ==================== Примечания ====================
# 
# 1. torchqrnn может быть не доступен в PyPI. Если так, установите из локального проекта:
#    cd C:\Users\psobo\Pycharm\pytorch-qrnn
#    pip install -e .
#
# 2. Для TensorFlow 2.10 на Windows может потребоваться Visual C++ Redistributable 2015-2022
#
# 3. Если возникают проблемы с Pillow, убедитесь, что установлены бинарные колеса:
#    pip install --only-binary :all: Pillow==10.4.0
#
# 4. Все версии протестированы на Python 3.10 и совместимы между собой
